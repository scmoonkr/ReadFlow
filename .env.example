# ReadFlow 환경 변수 예시 — 이 파일을 복사해 .env 로 만들어 실제 값을 채운다.
# .env 는 .gitignore 로 커밋 대상에서 제외된다 (비밀번호 노출 방지).

# MongoDB
MONGODB_ADDR = "host:port"
MONGO_USERNAME = "username"
MONGO_PWD = "password"
MONGODB_DB = LibrarySchool
MONGODB_READING = Reading

# API 서버 포트 (기본 9910)
# SERVER_PORT = 9910
